export const retrival = {
  //default number of files returned
  limit: 2,
};

export const sort = {
  //default number of files returned
  newest: ["created_at", "asc"],
  oldest: ["created_at", "desc"],
};
